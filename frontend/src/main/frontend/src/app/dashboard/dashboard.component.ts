import { Component } from '@angular/core';

/**
 * Main dashboard component invokes router outlet for logged user
 */
@Component({
    moduleId: module.id,
    selector: 'dashboard-cmp',
    templateUrl: 'dashboard.component.html'
})
export class DashboardComponent { }
